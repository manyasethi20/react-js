function App() {

  return (
    <h1>React with vite | Manya Sethi - Learnt about react with vite. Created it using npm create vite@latest. In package.json there are only two dependencies i.e. react and react-dom, in script used dev by writing npm run dev. There two main files in src folder i.e. App.jsx and main.jsx (deleted all other files like the css ones). In App.jsx made the header and removed all the packages which are not currently in use.</h1>
  )
}

export default App
